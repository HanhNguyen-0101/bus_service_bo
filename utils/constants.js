const DOMAIN = "http://localhost:3001";

const USER_TYPE = {
  ADMIN: "admin",
  CLIENT: "client",
  SUPER_ADMIN: "super admin",
};

const COMPANY_TYPE = {
  ONLINE: 'online',
  OFFLINE: 'offline'
}

const SECRET = 'secret';

module.exports = {
    USER_TYPE,
    COMPANY_TYPE,
    SECRET,
    DOMAIN
}